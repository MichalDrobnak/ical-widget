<span class="loader border-green-500"></span>

<style>
    .loader {
        width: 6rem;
        height: 6rem;
        border-width: 8px;
        border-style: solid;
        border-bottom-color: transparent;
        border-radius: 50%;
        display: inline-block;
        box-sizing: border-box;
        animation: rotation 1s linear infinite;
        }

        @keyframes rotation {
        0% {
            transform: rotate(0deg);
        }
        100% {
            transform: rotate(360deg);
        }
    }
</style>